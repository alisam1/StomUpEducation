{% extends 'base.html.twig' %}

{% block title %}Литература{% endblock %}

{% block body %}

<main class="main-center">
        {% if mobileDetect == true %}
        {{ include('main/books.mobile.twig') }}
        {% else %}
        {{ include('main/books.desktop.twig') }}
        {% endif %}

</main>

{% endblock %}
